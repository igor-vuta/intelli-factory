# Frontend Environment Variables
# Copy this file to .env.local and fill in actual values

# Backend API URL — used both by Next.js rewrites (server-side proxy) and
# referenced as a fallback. Must include the /api path segment.
# In development this defaults to http://localhost:8000/api.
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000/api
